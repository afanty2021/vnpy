# P0-2: 配置验证完善方案

## 问题描述

**文件**: `vnpy_china_config/global_config.py:221-224`

当前配置允许空字符串，但实际使用时QMT配置是必填项：

```python
class QmtConfig(BaseModel):
    account_id: str = ""
    mini_path: str = ""
    session_id: int = 0
    password: str = ""
```

**问题**：
1. QMT配置允许空字符串，但启动QMT网关时这些字段是必填的
2. 配置验证发生在使用时，而非启动时
3. 缺少跨模块配置依赖验证（如启用RPC时需要配置QMT）

## 修复方案

### 方案设计

1. **添加字段级验证**：使用Pydantic的`field_validator`
2. **添加依赖关系验证**：使用`model_validator`
3. **添加环境特定验证**：生产环境强制完整配置
4. **统一验证入口**：在`ConfigManager`中添加验证方法

### 实现步骤

#### 步骤1: 完善QmtConfig验证

修改 `vnpy_china_config/global_config.py`：

```python
"""
全局配置模块

定义系统级配置类，包括数据库、日志、RPC 和风控配置。
"""

from pathlib import Path
from typing import Optional, List
from pydantic import BaseModel, Field, field_validator, model_validator
from .base import BaseConfig, Environment


class QmtConfig(BaseModel):
    """QMT 交易接口配置

    统一管理 QMT/miniQMT 交易接口连接配置。

    Attributes:
        account_id: QMT 交易账号（必填）
        mini_path: miniQMT 路径（必填，必须是 userdata_mini 子目录）
        session_id: 会话ID（可选，用于多会话）
        password: 交易密码（可选，某些场景需要）
        enabled: 是否启用QMT（默认False，避免未配置时启动）

    Example:
        ```python
        qmt_config = QmtConfig(
            account_id="40218291",
            mini_path="D:/国金证券QMT交易端/userdata_mini/",
            enabled=True
        )
        ```
    """

    account_id: str = Field(
        default="",
        description="QMT交易账号（必填）",
        min_length=1
    )
    mini_path: str = Field(
        default="",
        description="miniQMT路径（必填，必须是userdata_mini子目录）",
        min_length=1
    )
    session_id: int = 0
    password: str = ""
    enabled: bool = False  # 默认禁用，避免未配置时启动

    @field_validator("account_id", "mini_path")
    @classmethod
    def validate_required_qmt_fields(cls, v: str, info) -> str:
        """验证QMT必填字段

        仅在enabled=True时验证必填，允许预配置但未启用的场景。
        """
        # 获取模型实例以检查enabled状态
        if hasattr(info, 'data') and isinstance(info.data, dict):
            enabled = info.data.get('enabled', False)
        else:
            # 如果无法获取enabled，检查当前值
            enabled = getattr(info.model, 'enabled', False) if hasattr(info, 'model') else False

        # 如果启用QMT，则account_id和mini_path必须非空
        if enabled and not v or not v.strip():
            raise ValueError(
                f"{info.field_name} 在启用QMT时不能为空。"
                f"请设置有效的{info.field_name}值，或设置enabled=False。"
            )
        return v.strip() if v else v

    @field_validator("mini_path")
    @classmethod
    def validate_mini_path(cls, v: str) -> str:
        """验证 miniQMT 路径"""
        if not v:
            return v

        v = v.strip()

        # 检查路径是否包含 userdata_mini
        if "userdata_mini" not in v and "USERDATA_MINI" not in v.upper():
            import warnings
            warnings.warn(
                f"miniQMT 路径可能不正确: {v}\n"
                "正确路径应包含 userdata_mini 子目录，例如:\n"
                "  D:/国金证券QMT交易端/userdata_mini/\n"
                "  /opt/QMT/userdata_mini/",
                UserWarning,
                stacklevel=2
            )

        # 检查路径是否存在（如果启用）
        # 注意：路径验证放在 model_validator 中进行，
        # 这里只做格式检查，避免在validator中进行IO操作

        return v

    @model_validator(mode='after')
    def validate_qmt_config(self) -> 'QmtConfig':
        """QMT配置整体验证

        验证：
        1. 启用时必填字段完整性
        2. 路径有效性（如果启用）
        """
        if self.enabled:
            # 检查必填字段
            errors: List[str] = []
            if not self.account_id or not self.account_id.strip():
                errors.append("account_id 不能为空")
            if not self.mini_path or not self.mini_path.strip():
                errors.append("mini_path 不能为空")

            if errors:
                raise ValueError(f"QMT配置错误: {', '.join(errors)}")

            # 检查路径是否存在
            mini_dir = Path(self.mini_path)
            if not mini_dir.exists():
                raise ValueError(
                    f"miniQMT 路径不存在: {self.mini_path}\n"
                    f"请确认路径正确或创建该目录。"
                )

        return self
```

#### 步骤2: 添加数据库配置验证

```python
class DatabaseConfig(BaseModel):
    """数据库配置

    统一管理 MySQL 和 Redis 数据库连接配置。

    Attributes:
        mysql_host: MySQL 主机地址
        mysql_port: MySQL 端口
        mysql_user: MySQL 用户名
        mysql_password: MySQL 密码
        mysql_database: MySQL 数据库名
        redis_host: Redis 主机地址
        redis_port: Redis 端口
        redis_db: Redis 数据库编号
        redis_password: Redis 密码
        pool_size: 数据库连接池大小
        max_overflow: 连接池最大溢出数
        enabled: 是否启用数据库功能

    Example:
        ```python
        db_config = DatabaseConfig(
            mysql_host="localhost",
            mysql_port=3306,
            mysql_user="root",
            mysql_password="password",
            mysql_database="vnpy_china"
        )
        ```
    """

    mysql_host: str = "localhost"
    mysql_port: int = 3306
    mysql_user: str = "vnpy"
    mysql_password: str = ""
    mysql_database: str = "vnpy_china"
    redis_host: str = "localhost"
    redis_port: int = 6379
    redis_db: int = 0
    redis_password: str = ""
    pool_size: int = 5
    max_overflow: int = 10
    enabled: bool = True  # 默认启用

    @field_validator("mysql_port", "redis_port")
    @classmethod
    def validate_port(cls, v: int) -> int:
        """验证端口号"""
        if v <= 0 or v > 65535:
            raise ValueError(f"端口号必须在 1-65535 之间，当前值: {v}")
        return v

    @field_validator("pool_size", "max_overflow")
    @classmethod
    def validate_pool_size(cls, v: int) -> int:
        """验证连接池大小"""
        if v <= 0:
            raise ValueError(f"连接池大小必须大于0，当前值: {v}")
        if v > 100:
            raise ValueError(f"连接池大小不应超过100，当前值: {v}")
        return v

    @model_validator(mode='after')
    def validate_database_config(self) -> 'DatabaseConfig':
        """数据库配置整体验证"""
        if self.enabled:
            # 检查必填字段
            if not self.mysql_database:
                raise ValueError("启用数据库时，mysql_database 不能为空")

            # 检查密码（生产环境警告）
            from .base import Environment
            # 这里需要通过某种方式获取当前环境
            # 简化处理：如果使用默认密码，发出警告
            if self.mysql_password == "" or self.mysql_password == "password":
                import warnings
                warnings.warn(
                    "MySQL使用空密码或默认密码，生产环境请设置强密码！",
                    UserWarning,
                    stacklevel=2
                )

        return self
```

#### 步骤3: 添加GlobalConfig交叉验证

```python
class GlobalConfig(BaseConfig):
    """全局配置

    系统级配置的顶层配置类，整合所有全局配置项。
    作为配置层次结构的根节点。

    Attributes:
        environment: 运行环境
        database: 数据库配置
        logging: 日志配置
        rpc: RPC 配置
        qmt: QMT 交易接口配置
        risk: 风控全局参数
        work_dir: 工作目录
        data_dir: 数据目录

    Example:
        ```python
        global_config = GlobalConfig(
            environment=Environment.PRODUCTION,
            database=DatabaseConfig(mysql_host="prod-db.example.com"),
            logging=LoggingConfig(level="WARNING")
        )
        ```
    """

    environment: Environment = Environment.DEVELOPMENT
    database: DatabaseConfig = Field(default_factory=DatabaseConfig)
    logging: LoggingConfig = Field(default_factory=LoggingConfig)
    rpc: RpcConfig = Field(default_factory=RpcConfig)
    qmt: QmtConfig = Field(default_factory=QmtConfig)
    risk: RiskGlobalConfig = Field(default_factory=RiskGlobalConfig)
    work_dir: Path = Field(default_factory=lambda: Path(".vntrader_china"))
    data_dir: Path = Field(default_factory=lambda: Path("data"))

    @model_validator(mode='after')
    def validate_cross_module_dependencies(self) -> 'GlobalConfig':
        """跨模块依赖验证

        验证：
        1. 启用RPC时需要配置QMT
        2. 启用数据存储时需要配置数据库
        3. 生产环境强制完整配置
        """
        # RPC依赖QMT
        # 如果启用了RPC服务端，则需要QMT配置
        rpc_server_enabled = getattr(self, 'rpc_server_enabled', False)
        if rpc_server_enabled and not self.qmt.enabled:
            raise ValueError(
                "启用RPC服务端时需要配置QMT。\n"
                "请在global_{self.environment.value}.yaml中设置：\n"
                "  qmt:\n"
                "    enabled: true\n"
                "    account_id: \"your_account_id\"\n"
                "    mini_path: \"path/to/userdata_mini\""
            )

        # 生产环境严格检查
        if self.environment == Environment.PRODUCTION:
            warnings: List[str] = []

            if not self.database.enabled:
                warnings.append("生产环境建议启用数据库功能")

            if self.qmt.enabled and not self.qmt.password:
                warnings.append("生产环境建议设置QMT交易密码")

            if warnings:
                import warnings as py_warnings
                for msg in warnings:
                    py_warnings.warn(msg, UserWarning, stacklevel=2)

        return self

    def get_mysql_dsn(self) -> str:
        """获取 MySQL DSN 连接字符串

        Returns:
            MySQL DSN 字符串
        """
        return (
            f"mysql+pymysql://{self.database.mysql_user}:{self.database.mysql_password}"
            f"@{self.database.mysql_host}:{self.database.mysql_port}/{self.database.mysql_database}"
        )

    def get_redis_url(self) -> str:
        """获取 Redis 连接 URL

        Returns:
            Redis 连接 URL
        """
        if self.database.redis_password:
            return f"redis://:{self.database.redis_password}@{self.database.redis_host}:{self.database.redis_port}/{self.database.redis_db}"
        return f"redis://{self.database.redis_host}:{self.database.redis_port}/{self.database.redis_db}"

    def validate_for_use(self, feature: str) -> None:
        """验证特定功能所需的配置

        Args:
            feature: 功能名称（qmt, database, rpc等）

        Raises:
            ValueError: 配置不满足要求
        """
        if feature == "qmt":
            if not self.qmt.enabled:
                raise ValueError("QMT功能未启用，请在配置中设置 qmt.enabled=true")
            if not self.qmt.account_id or not self.qmt.mini_path:
                raise ValueError("QMT配置不完整，请检查account_id和mini_path")

        elif feature == "database":
            if not self.database.enabled:
                raise ValueError("数据库功能未启用")
            if not self.database.mysql_database:
                raise ValueError("数据库名称未配置")

        elif feature == "rpc_server":
            if not self.qmt.enabled:
                raise ValueError("RPC服务端需要QMT配置")
```

#### 步骤4: 在ConfigManager中添加启动验证

修改 `vnpy_china_config/loader.py`：

```python
class ConfigManager:
    """配置管理器（单例模式）"""

    # ... 现有代码 ...

    def load_global_config(self, force_reload: bool = False) -> BaseConfig:
        """加载全局配置

        Args:
            force_reload: 是否强制重新加载

        Returns:
            全局配置对象
        """
        if not force_reload and "global" in self._configs:
            return self._configs["global"]

        from .global_config import GlobalConfig

        config_file = self._config_path / f"global_{self._environment.value}.yaml"

        if config_file.exists():
            try:
                config = GlobalConfig.from_file(config_file)
            except Exception as e:
                # 配置验证失败，提供友好错误信息
                raise ValueError(
                    f"配置文件 {config_file} 验证失败:\n{e}\n\n"
                    f"请检查配置文件格式和必填字段。"
                )
        else:
            # 创建默认配置并保存
            config = GlobalConfig()
            config.environment = self._environment
            config.to_file(config_file)
            logger.warning(
                f"配置文件不存在，已创建默认配置: {config_file}\n"
                f"请根据需要修改配置。"
            )

        self._configs["global"] = config
        return config

    def validate_config_for_feature(self, feature: str) -> None:
        """验证功能所需配置

        在使用特定功能前调用，确保配置正确。

        Args:
            feature: 功能名称

        Raises:
            ValueError: 配置不满足要求

        Example:
            ```python
            manager = ConfigManager()
            config = manager.load_global_config()

            # 启动QMT前验证
            manager.validate_config_for_feature("qmt")
            ```
        """
        config = self.get_config("global")
        if config is None:
            raise ValueError("全局配置未加载")

        if hasattr(config, 'validate_for_use'):
            config.validate_for_use(feature)
        else:
            # 向后兼容
            if feature == "qmt":
                if not config.qmt.enabled:
                    raise ValueError("QMT功能未启用")
```

### 测试方法

创建 `tests/test_config_validation.py`：

```python
"""测试配置验证功能"""

import pytest
from pathlib import Path
from pydantic import ValidationError

from vnpy_china_config.global_config import QmtConfig, DatabaseConfig, GlobalConfig
from vnpy_china_config.base import Environment


def test_qmt_config_disabled_allows_empty():
    """测试禁用QMT时允许空配置"""
    config = QmtConfig(enabled=False)
    assert config.account_id == ""
    assert config.mini_path == ""


def test_qmt_config_enabled_requires_fields():
    """测试启用QMT时必填字段验证"""
    with pytest.raises(ValidationError) as exc_info:
        QmtConfig(enabled=True, account_id="", mini_path="")

    errors = exc_info.value.errors()
    error_messages = [e['msg'] for e in errors]
    assert any("account_id" in msg for msg in error_messages)
    assert any("mini_path" in msg for msg in error_messages)


def test_qmt_config_mini_path_validation():
    """测试mini_path路径验证"""
    # 正确路径格式
    config = QmtConfig(
        enabled=True,
        account_id="40218291",
        mini_path="D:/QMT/userdata_mini/"
    )
    assert "userdata_mini" in config.mini_path

    # 错误路径格式（警告但不报错，因为可能还没创建目录）
    with pytest.warns(UserWarning):
        QmtConfig(
            enabled=True,
            account_id="40218291",
            mini_path="D:/QMT/"  # 缺少userdata_mini
        )


def test_database_config_password_warning():
    """测试数据库密码警告"""
    with pytest.warns(UserWarning, match="空密码或默认密码"):
        DatabaseConfig(
            enabled=True,
            mysql_password=""
        )


def test_global_config_production_warnings():
    """测试生产环境配置警告"""
    with pytest.warns(UserWarning):
        GlobalConfig(
            environment=Environment.PRODUCTION,
            qmt=QmtConfig(enabled=True, password="")
        )


def test_validate_for_use():
    """测试功能使用前验证"""
    config = GlobalConfig(
        qmt=QmtConfig(enabled=False)
    )

    # QMT未启用，应该报错
    with pytest.raises(ValueError, match="QMT功能未启用"):
        config.validate_for_use("qmt")

    # 启用QMT但配置不完整
    config.qmt.enabled = True
    with pytest.raises(ValueError, match="QMT配置不完整"):
        config.validate_for_use("qmt")


def test_config_file_loading_with_validation():
    """测试配置文件加载时的验证"""
    from vnpy_china_config.loader import ConfigManager

    manager = ConfigManager()

    # 加载有效配置
    config = manager.load_global_config()
    assert config is not None

    # 验证功能配置
    if config.qmt.enabled:
        # 应该能通过验证
        manager.validate_config_for_feature("qmt")


if __name__ == "__main__":
    pytest.main([__file__, "-v"])
```

### 配置文件示例

创建 `examples/client_server/global_config.yaml.example`：

```yaml
# 全局配置示例
# 复制此文件为 global_development.yaml 或 global_production.yaml

environment: development  # development, testing, production

# 数据库配置
database:
  enabled: true
  mysql_host: localhost
  mysql_port: 3306
  mysql_user: vnpy
  mysql_password: ""  # 生产环境请设置强密码
  mysql_database: vnpy_china
  redis_host: localhost
  redis_port: 6379
  redis_db: 0
  redis_password: ""
  pool_size: 5
  max_overflow: 10

# 日志配置
logging:
  level: INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
  file_enabled: true
  file_path: logs/vnpy_china.log
  max_bytes: 10485760  # 10MB
  backup_count: 5
  console_enabled: true

# RPC配置
rpc:
  rep_address: "tcp://127.0.0.1:2014"
  pub_address: "tcp://127.0.0.1:4102"
  timeout: 5000

# QMT配置
qmt:
  enabled: false  # 默认禁用，需要时设置为true
  account_id: ""  # 启用时必填
  mini_path: ""  # 启用时必填，例如: D:/国金证券QMT交易端/userdata_mini/
  session_id: 0
  password: ""

# 风控全局参数
risk:
  max_position_ratio: 0.8  # 最大持仓比例
  max_single_position_ratio: 0.2  # 单只股票最大持仓比例
  max_daily_loss_ratio: 0.05  # 单日最大亏损比例
  max_consecutive_losses: 5  # 最大连续亏损次数

# 工作目录
work_dir: .vntrader_china
data_dir: data
```

### 迁移步骤

1. **更新依赖**
   ```bash
   # 确保使用最新版本的Pydantic
   conda run -n Quant-3.11 pip install --upgrade pydantic>=2.0
   ```

2. **备份现有配置**
   ```bash
   cp .vntrader_china/config/global_development.yaml .vntrader_china/config/global_development.yaml.backup
   ```

3. **更新配置文件**
   ```bash
   # 将新的配置示例复制到配置目录
   cp examples/client_server/global_config.yaml.example .vntrader_china/config/global_development.yaml
   ```

4. **编辑配置**
   ```yaml
   # 根据需要修改配置
   qmt:
     enabled: true  # 如果使用QMT
     account_id: "your_account_id"
     mini_path: "path/to/userdata_mini"
   ```

5. **验证配置**
   ```bash
   conda run -n Quant-3.11 python -c "
   from vnpy_china_config import ConfigManager
   manager = ConfigManager()
   config = manager.load_global_config()
   print('配置加载成功:', config.environment)
   "
   ```

### 验收标准

- [ ] 禁用QMT时允许空配置
- [ ] 启用QMT时验证必填字段
- [ ] 生产环境发出安全警告
- [ ] 配置文件加载失败时提供友好错误信息
- [ ] 功能使用前验证正常工作

---

**工作量估算**: 0.5人日
**优先级**: P0（必须修复）
**负责人**: 待分配
